{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  "ConnectionStrings": {
    "FileMetadataDatabase": "Host={host}; Username={username}; Password={password}; Database={dbName}",
    "EntityIdSequenceDatabase": "Host={host}; Username={username}; Password={password}; Database={dbName}"
  },
  "IdSequenceName": "id_seq",
  "RootDirectory": "./upload"
}